# [phases.setup]
#     nixPkgs = ['...', 'ffmpeg']
[phases.setup]
nixPkgs = ["python3", "ffmpeg", "cairo", "pango", "gobject-introspection", "glib"]

[phases.install]
cmds = ["python3 -m pip install --no-cache-dir -r requirements.txt"]

[start]
cmd = "python3 bot.py"
